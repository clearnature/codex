[package]
name = "codex-i18n-check"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "codex-i18n-check"
path = "src/main.rs"

[lints]
workspace = true

[dependencies]
# Parsing the approval-template assets: they are data, so a translated copy has
# to be reconciled against the English one (`[asset]` column in the report).
serde_json = { workspace = true }

[dev-dependencies]
pretty_assertions = { workspace = true }
tempfile = { workspace = true }
