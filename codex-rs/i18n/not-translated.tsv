# 已判定**不译**、但**会被渲染**的文案（key<TAB>file:line<TAB>理由）
#
# 消费者：`codex-i18n-check` —— 它扫「已包 tr 的字面量」，这些串会被算作 rendered key，
# 但没有词条 ⇒ 若不登记就会**永远报 missing**（missing 全零是核心验收判据）。
# 每条必须写清「为什么不能译」；条数会被打在报告里，豁免是**可见**的。
Allow and don't ask me again	codex-rs/core/src/mcp_tool_call.rs:1378	选项 label 同时是比对值：TUI 原样提交该文本（request_user_input/mod.rs 的 submit_answers），core 再用 == 与英文 const 比对（mcp_tool_call.rs:2088）⇒ 译了静默破坏 ApprovedMcpPolicyAmendment
Install	codex-rs/core/src/mcp_skill_dependencies.rs:42	同上：label 被提交后与 MCP_DEPENDENCY_OPTION_INSTALL 比对（:336）
Continue anyway	codex-rs/core/src/mcp_skill_dependencies.rs:43	同上：与 MCP_DEPENDENCY_OPTION_SKIP 比对
clock	codex-rs/core/src/tools/handlers/sleep.rs:26	常量 `NAMESPACE`：它是**工具命名空间名**（:47/:67 拼进 ToolSpec/协议发给模型）——改名就破坏协议，不能译（消费者：codex-i18n-check 的 bound keys；本文件此轮才首次含 tr 调用，因此这条此前不可见）
sleep	codex-rs/core/src/tools/handlers/sleep.rs:27	常量 `TOOL_NAME`：**工具名**（:50/:67 进 ToolSpec 与 ToolName::namespaced）——模型按名字调用工具，译了工具就调不通（同上，本文件此轮才被扫描）
wait_for_environment	codex-rs/core/src/tools/handlers/wait_for_environment.rs:20	常量 `WAIT_FOR_ENVIRONMENT_TOOL_NAME`：**工具名**，模型按名字调用工具，译了工具就调不通 ⇒ §4 决策 3 / §12.7（消费者：codex-i18n-check 的「会被渲染的字符串常量」扫描；本文件此轮才首次含 tr 调用/引用 codex_i18n，因此这几条此前不可见 —— 与 sleep.rs 的 `clock`/`sleep` 两条同因）
Wait for a selected execution environment marked as `starting` to become available. Use this when the current task needs that environment's files, commands, or installed capabilities. Do not wait if the task can be completed using tools already available, such as connectors. Waiting may take several minutes and blocks other tool calls. If startup fails, continue without that environment.	codex-rs/core/src/tools/handlers/wait_for_environment.rs:21	常量 `DEFAULT_TOOL_DESCRIPTION`：**工具描述**（进 `ToolSpec` 随 ResponsesApiTool 发给模型），译了改变模型行为 ⇒ §4 决策 3 / §12.2（消费者：codex-i18n-check 的「会被渲染的字符串常量」扫描；本文件此轮才首次含 tr 调用/引用 codex_i18n，因此这几条此前不可见 —— 与 sleep.rs 的 `clock`/`sleep` 两条同因）
The exact environment ID marked as `starting` in `<environment_context>`.	codex-rs/core/src/tools/handlers/wait_for_environment.rs:23	常量 `DEFAULT_ENVIRONMENT_ID_DESCRIPTION`：**工具参数说明**（同上，喂模型）⇒ §4 决策 3 / §12.2（消费者：codex-i18n-check 的「会被渲染的字符串常量」扫描；本文件此轮才首次含 tr 调用/引用 codex_i18n，因此这几条此前不可见 —— 与 sleep.rs 的 `clock`/`sleep` 两条同因）
