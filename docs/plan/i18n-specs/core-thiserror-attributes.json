{
 "batches": [
  {
   "file": "codex-rs/core/src/unified_exec/errors.rs",
   "root": "codex-rs/core/src",
   "translate": {},
   "register": {},
   "extra_edits": [
    {
     "find": "    #[error(\"Failed to create unified exec process: {message}\")]",
     "replace": "#[error(\"{}\", tr_with(current(), \"Failed to create unified exec process: {0}\", &[message]))]",
     "count": 1,
     "note": "Failed to create unified exe"
    },
    {
     "find": "    #[error(\"Unified exec process failed: {message}\")]",
     "replace": "#[error(\"{}\", tr_with(current(), \"Unified exec process failed: {0}\", &[message]))]",
     "count": 1,
     "note": "Unified exec process failed:"
    },
    {
     "find": "    #[error(\"Unknown process id {process_id}\")]",
     "replace": "#[error(\"{}\", tr_with(current(), \"Unknown process id {0}\", &[&process_id.to_string()]))]",
     "count": 1,
     "note": "Unknown process id {0}"
    },
    {
     "find": "    #[error(\"stdin approval failed: {0:?}\")]",
     "replace": "#[error(\"{}\", tr_with(current(), \"stdin approval failed: {0}\", &[&format!(\"{:?}\", _0)]))]",
     "count": 1,
     "note": "stdin approval failed: {0}"
    },
    {
     "find": "    #[error(\"failed to write to stdin\")]",
     "replace": "#[error(\"{}\", tr(current(), \"failed to write to stdin\"))]",
     "count": 1,
     "note": "failed to write to stdin"
    },
    {
     "find": "    #[error(\n        \"stdin is closed for this session; rerun exec_command with tty=true to keep stdin open\"\n    )]",
     "replace": "#[error(\"{}\", tr(current(), \"stdin is closed for this session; rerun exec_command with tty=true to keep stdin open\"))]",
     "count": 1,
     "note": "stdin is closed for this ses"
    },
    {
     "find": "    #[error(\"missing command line for unified exec request\")]",
     "replace": "#[error(\"{}\", tr(current(), \"missing command line for unified exec request\"))]",
     "count": 1,
     "note": "missing command line for uni"
    },
    {
     "find": "    #[error(\"Command denied by sandbox: {message}\")]",
     "replace": "#[error(\"{}\", tr_with(current(), \"Command denied by sandbox: {0}\", &[message]))]",
     "count": 1,
     "note": "Command denied by sandbox: {"
    },
    {
     "find": "    #[error(\"{path} is not valid on {}\", std::env::consts::OS)]",
     "replace": "#[error(\"{}\", tr_with(current(), \"{0} is not valid on {1}\", &[&path.to_string(), std::env::consts::OS]))]",
     "count": 1,
     "note": "{0} is not valid on {1}"
    }
   ],
   "extra_dict": {
    "Failed to create unified exec process: {0}": "创建统一执行进程失败：{0}",
    "Unified exec process failed: {0}": "统一执行进程失败：{0}",
    "Unknown process id {0}": "未知的进程ID {0}",
    "stdin approval failed: {0}": "标准输入审批失败：{0}",
    "failed to write to stdin": "写入标准输入失败",
    "stdin is closed for this session; rerun exec_command with tty=true to keep stdin open": "本会话的标准输入已关闭；要保留标准输入，请用tty=true重新运行exec_command",
    "missing command line for unified exec request": "统一执行请求缺少命令行",
    "Command denied by sandbox: {0}": "命令被沙箱拒绝：{0}",
    "{0} is not valid on {1}": "{0} 在 {1} 上无效"
   }
  },
  {
   "file": "codex-rs/core/src/mcp_tool_call/account.rs",
   "root": "codex-rs/core/src",
   "translate": {},
   "register": {},
   "extra_edits": [
    {
     "find": "    #[error(\"This app tool requires a non-empty string link_id argument\")]",
     "replace": "    #[error(\"{}\", tr(current(), \"This app tool requires a non-empty string link_id argument\"))]",
     "count": 1,
     "note": "account.rs:11"
    }
   ],
   "extra_dict": {
    "This app tool requires a non-empty string link_id argument": "此应用工具要求link_id参数为非空字符串"
   }
  }
 ]
}